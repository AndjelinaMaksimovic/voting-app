apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: voting-app

build:
  artifacts:
    - image: voting-app
      docker:
        dockerfile: Dockerfile

deploy:
  kubectl:
    manifests:
      - k8s/*.yaml
